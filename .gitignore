node_modules
dist
.astro
.DS_Store
.env
.env.local
.env.production
.vercel
.netlify

# Build-time generated data. Re-derived from data/apps/*.yml on every
# build and every GitHub Actions refresh — never commit it.
data/generated/

# IDE / editor cruft
.idea/
.vscode/
*.swp
*.swo

# OS / shell
Thumbs.db

# One-off dev artifacts and large binary assets not needed at runtime
/art/
/docs/  # reserved for future design docs; currently empty
*.log

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
